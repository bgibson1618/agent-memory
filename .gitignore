# agent-roster observable run dirs (delegate scratch — never committed)
work/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# local env
.env

# KodOS per-session machine state (SessionStart hook + statusline sensor); never committed
.kodos/runtime.json
.kodos/ctx-*.json
.kodos/hook-fire.log
# KodOS checkpoint machine state (F23 last-CLEAN marker); never committed
.kodos/sanity.last-clean
.kodos/handoff.latch
