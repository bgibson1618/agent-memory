# one-shots

Spent, already-applied scripts kept for provenance — do NOT re-run against the
live KB. Evergreen operational tools stay one level up in `scripts/`.

- `backfill-source.py` — D9 source backfill, applied 2026-07-24 (re-runnable by
  design, but there is nothing left to backfill; every concept carries `source:`).
