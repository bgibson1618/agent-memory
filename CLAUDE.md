# agent-memory

> This is a KodOS project. Before editing, read `PRD.md`, `ARCHITECTURE.md`, `FEATURES.md` (the plan), and `IMPLEMENTATION.md` (current state) when present — or run `/kodos:go` to orient. `DECISION_LOG.md` records why things are the way they are. There is no `CONTEXT.md`; KodOS owns the read order.
